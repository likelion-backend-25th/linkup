<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.likelion.bebc25.linkup.report.mapper.ReportMapper">
    <!-- 게시글 신고 등록 -->
    <insert id="saveReport">
        INSERT INTO report(
            member_id,
            target_id,
            post_id,
            target_type,
            reason,
            content,
            status,
            created_at,
            updated_at
        )

        VALUES (
            #{memberId},
            #{postId},
            #{postId},
            'POST',
            #{request.reason},
            #{request.content},
            'WAIT',
            NOW(),
            NOW()
        )
    </insert>
</mapper>