<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.likelion.bebc25.linkup.reply.mapper.ReplyMapper">
    <select id="getByPostId" resultType="ReplyResponse">
        SELECT
        r.id AS id,
        m.id AS memberId,
        m.name,
        m.unique_id,
        m.profile_image,
        r.content,
        r.like_count,
        r.created_at,
        r.updated_at
        FROM reply r
        JOIN post p ON r.post_id = p.id
        JOIN member m ON r.member_id = m.id
        <where>
            AND r.post_id = #{postId}
            <if test="cursor != null">
                AND r.id &gt; #{cursor}
            </if>
        </where>
        ORDER BY r.id ASC
        LIMIT #{limit}
    </select>
</mapper>